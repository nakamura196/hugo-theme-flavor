{
  "scripts": {
    "build": "PATH=$PWD/node_modules/.bin:$PATH hugo --minify && npx pagefind --site public",
    "dev": "PATH=$PWD/node_modules/.bin:$PATH hugo server"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0",
    "tailwindcss": "^4.0",
    "pagefind": "^1.3.0"
  }
}
